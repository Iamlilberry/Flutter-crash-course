void main(){
  // Strings 
  String username = "AhmBerry";
  //  Integers
  int myage = 20;
  // bool(true/false)
  bool isverfied = true;
  // const -  value must be set at compile time & cannot be changed
  const age = 30;
  // final - setting a compile time value is optional & can be changed
  // final age;
  // age = 20;
}